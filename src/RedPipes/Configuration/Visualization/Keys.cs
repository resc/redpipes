namespace RedPipes.Configuration.Visualization
{
    /// <summary> Recognized node labels that the different visualizers can use </summary>
    public static class Keys
    {
        /// <summary> The primary text to be shown for an node. </summary>
        public const string Name = nameof(Name);
    }
}
